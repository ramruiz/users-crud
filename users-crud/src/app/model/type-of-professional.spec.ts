import { TypeOfProfessional } from './type-of-professional';

describe('TypeOfProfessional', () => {
  it('should create an instance', () => {
    expect(new TypeOfProfessional()).toBeTruthy();
  });
});
